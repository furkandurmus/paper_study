# Tested baseline for this repo on Python 3.11 and recent NVIDIA GPUs.
# Installs CUDA 12.8 PyTorch wheels from the official PyTorch index.
--extra-index-url https://download.pytorch.org/whl/cu128

# PyTorch stack
torch==2.10.0+cu128
torchvision==0.25.0+cu128
torchaudio==2.10.0+cu128
triton==3.6.0

# Hugging Face / training
transformers==5.3.0
accelerate==1.13.0
peft==0.18.1
trl==0.29.0
datasets==4.6.1
deepspeed==0.18.7
bitsandbytes>=0.46.1

# Core utilities
numpy==2.1.2
Pillow==10.2.0
tqdm==4.66.5
requests>=2.32,<3

# Logging / tracking
tensorboard>=2.16,<3
wandb>=0.25,<0.26

# Evaluation
rouge-score>=0.1.2,<0.2
sacrebleu>=2.4,<3
nltk>=3.9,<4

# Development
pytest>=8,<9
black>=24,<26

# Optional extras
# flash-attn is optional and should be installed separately if you specifically want it:
# pip install flash-attn --no-build-isolation
