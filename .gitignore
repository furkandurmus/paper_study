# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
ENV/
env/
.venv/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Jupyter
.ipynb_checkpoints/
*.ipynb

# Data (large files)
data/images/
data/*.png
data/*.jpg
data/*.jpeg
data/*.dcm
data/*.nii
data/*.nii.gz

# Outputs
outputs/
checkpoints/
logs/
wandb/
*.log

# ── Kept LOCAL only — NOT part of this CTA-MIP code repo ──
# Separate studies (each gets its own repo): ISLES'26/ATLAS and the ISLES'22 DWI arm.
mri_dwi_study/
isles26_study/
# Teaching / scratch scripts written while learning.
learn/
# Preprocessing image outputs + PATIENT DATA (PHI) — must NEVER be committed.
mip_images/
preprocessed_brains/
# Thesis / progress documents (keep local).
*.pptx
*.pdf
*.docx
*.html
# Windows download markers, tooling index, and the stray brace-expansion dir.
*Zone.Identifier
.codegraph/
{data,src,scripts,configs,outputs}/
# Raw DICOM + patient scans (PHI in the files AND the DICOM headers) — NEVER commit.
*.dcm
raw_stroke_images/
mip_images*/
# Brain volumes (NRRD/NIfTI) anywhere — patient scans, never commit.
*.nrrd
*.nii
*.nii.gz
preprocessed*/
preperocessed*/

# Model weights (large files)
*.bin
*.safetensors
*.pt
*.pth
*.ckpt

# OS
.DS_Store
Thumbs.db

# Temporary files
*.tmp
*.bak
*.cache
